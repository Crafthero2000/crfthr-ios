# AGENTS

- Всегда отвечай на русском.
- Все shell-команды должны выполняться в Git Bash (Git for Windows).
- Используется bash, а не PowerShell и не cmd.
- Пути только POSIX-стиля.
- Коммит считается завершённым только после git push.
- Запуск CI запрещён, если последний коммит не запушен.
